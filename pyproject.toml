[project]
name = "clacks"
version = "0.1.0"
description = "A lightweight relay server that leverages email and SMS capabilities to enable querying the google maps API over text messages"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.32.5",
]
